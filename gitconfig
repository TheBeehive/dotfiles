### ~/.gitconfig: User configuration for `git`

[include]
	path = .gitconfig_user

[core]
	excludesfile = ~/.gitignore
	pager = less -FXRS -x4

[alias]
	append = commit --amend --no-edit
	br = branch
	co = checkout
	st = status
