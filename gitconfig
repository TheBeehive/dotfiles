### ~/.gitconfig: User configuration for `git`

[include]
  path = .gitconfig_user

[core]
  excludesfile = ~/.gitignore

[color]
  ui = auto

[push]
  default = simple

### ~/.gitconfig: User configuration for `git`
